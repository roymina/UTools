# UTools Example

The original example scenes and scripts under `Assets/UTools/Example` act as the reference sample set for:

- recommended `UDIContext`
- `UMessageCenter`
- `UFind` auto binding

When extracting this folder into a standalone package, move the example assets into this sample directory.
